{
  "schema_version": 2,
  "purpose": "formal",
  "short": {
    "queries": [
      "SELECT SUM(i) FROM TABLE(generate_series(1, 1000)) AS t(i)",
      "SELECT COUNT(*) FROM TABLE(generate_series(1, 1000)) AS t(i) WHERE i % 7 = 0"
    ],
    "plan_contains": ["Physical"],
    "windows": [
      {"concurrency": 1, "duration_ms": 120000, "warmup_ms": 5000, "repetitions": 5},
      {"concurrency": 8, "duration_ms": 120000, "warmup_ms": 5000, "repetitions": 5},
      {"concurrency": 32, "duration_ms": 120000, "warmup_ms": 5000, "repetitions": 5}
    ]
  },
  "mixed": {
    "fixture": {
      "kind": "iceberg-rest",
      "rows_per_file": 4096,
      "files_per_table": 4,
      "foreground_rows": 65536
    },
    "foreground_clients": 8,
    "duration_ms": 120000,
    "repetitions": 5,
    "job_timeout_ms": 60000,
    "poll_interval_ms": 100,
    "producers": [
      {
        "kind": "mv-refresh",
        "jobs": 256,
        "minimum_completions": 1
      },
      {
        "kind": "analyze",
        "jobs": 256,
        "minimum_completions": 1
      },
      {
        "kind": "optimize",
        "jobs": 256,
        "minimum_completions": 1
      }
    ]
  },
  "slow_output": {
    "query": "SELECT i, repeat('x', 1024) FROM TABLE(generate_series(1, 1000000)) AS t(i)",
    "bytes_per_interval": 65536,
    "interval_ms": 100,
    "duration_ms": 120000
  }
}

{
  "schema_version": 2,
  "purpose": "smoke",
  "short": {
    "queries": ["SELECT SUM(i) FROM TABLE(generate_series(1, 100)) AS t(i)"],
    "plan_contains": [],
    "windows": [
      {"concurrency": 1, "duration_ms": 250, "warmup_ms": 25, "repetitions": 1},
      {"concurrency": 2, "duration_ms": 250, "warmup_ms": 25, "repetitions": 1}
    ]
  },
  "mixed": {
    "fixture": {
      "kind": "iceberg-rest",
      "rows_per_file": 8,
      "files_per_table": 2,
      "foreground_rows": 32
    },
    "foreground_clients": 2,
    "duration_ms": 30000,
    "repetitions": 1,
    "job_timeout_ms": 60000,
    "poll_interval_ms": 50,
    "producers": [
      {
        "kind": "mv-refresh",
        "jobs": 1,
        "minimum_completions": 1
      },
      {
        "kind": "analyze",
        "jobs": 1,
        "minimum_completions": 1
      },
      {
        "kind": "optimize",
        "jobs": 1,
        "minimum_completions": 1
      }
    ]
  },
  "slow_output": {
    "query": "SELECT i, repeat('x', 1024) FROM TABLE(generate_series(1, 10000)) AS t(i)",
    "control_query": "SELECT 1",
    "foreground_query": "SELECT SUM(i) FROM TABLE(generate_series(1, 100)) AS t(i)",
    "bytes_per_interval": 4096,
    "interval_ms": 20,
    "duration_ms": 300,
    "repetitions": 1
  }
}

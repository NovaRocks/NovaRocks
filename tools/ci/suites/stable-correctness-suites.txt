# Stable correctness suites for tools/ci/local-full-ci.sh default daily mode.
# Keep this list conservative. Add a suite only after a serial local run
# (`novarocks-sql-test -j 1`) has passed in the current environment.
filter
limit
project
sort
join
cte
set-op
table-function
runtime-filter
low-cardinality
complex-type
function
aggregate
# The default stable runner skips analytic's two exhaustive migrated window matrices;
# run `--suite analytic` explicitly when full window-matrix coverage is needed.
analytic
optimizer
optimizer-dist
statistics
iceberg
iceberg-compatibility
iceberg-ddl
iceberg-dml
iceberg-ivm
iceberg-mv-apply
iceberg-rest
distributed-resilience
paimon
materialized-view
session

# The suites below are not part of default daily mode yet.
# iceberg-mv-scheduler uses the canonical FE/BE pair. The generated FE config
#   enables the MV-refresh scheduler, but the async cases remain timing-sensitive
#   and are excluded from the default daily run.
# decimal tests DECIMAL256 (precision 39-76); the standalone Arrow engine caps at Decimal128
#   (precision 38), so it fails on native too. Excluded until DECIMAL256 is supported.

[package]
name = "novarocks-failpoint"
version.workspace = true
edition.workspace = true
license.workspace = true
publish = false

[lints]
workspace = true

[features]
default = ["typed"]
typed = ["dep:novarocks-types"]

[dependencies]
novarocks-types = { workspace = true, optional = true }

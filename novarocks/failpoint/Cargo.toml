[package]
name = "novarocks-failpoint"
version = "0.1.0"
edition = "2024"
publish = false

[features]
default = ["typed"]
typed = ["dep:novarocks-proto", "dep:novarocks-types"]

[dependencies]
novarocks-proto = { path = "../proto", optional = true }
novarocks-types = { path = "../types", optional = true }
